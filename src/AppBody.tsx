import React from "react";
import styled from "styled-components";

const AppBodyContainer = styled.div`
  flex: 1;
`;
const AppBody = () => {
  return <AppBodyContainer>body</AppBodyContainer>;
};

export default AppBody;
